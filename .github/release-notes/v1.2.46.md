# v1.2.46

- 系统版本窗口恢复简洁更新体验：保留“立即更新”，不再展示更新日志、更新详情或外部发布地址。
- PTR 反向 DNS 改为检测弹窗内的提醒项，不再影响域名 DNS 正常状态。
- 群发启动只要求 MX、SPF、DKIM、DMARC 通过，使用中继时不会因 PTR 提醒被拦截。
- 新建群发恢复附件选择，保存草稿或开始发送时都会保留附件并随邮件发送。
- 优化群发正文放大模式，放大后正文区域占满弹窗主区域，编辑空间更大。
- 正式镜像增加版本号兜底，避免系统版本显示为 `dev`。
